# SimBench sample

Use: realistic benchmark distribution networks and time-series scenarios after IEEE 33-bus baseline.

Source package downloaded from PyPI using: python -m pip download simbench --no-deps

For later experiments, install with dependencies when environment allows:
python -m pip install simbench pandapower

Then call simbench.get_simbench_net(...).
